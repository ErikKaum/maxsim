# Nix build outputs
/result
/result-*
# Symlink we create to point at `result` so kernels.get_local_kernel("build") works.
/build
# Local Docker-fallback output (see `just cuda-release-local`).
/build-cuda

# Python bytecode / packaging
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg

# Test / type-checker caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/
.venv-*/
venv/
env/

# macOS
.DS_Store

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo

# direnv
.direnv/

# Logs / scratch
*.log

# Dev-only state, never published
/plan.md
/.claude/
/.hypothesis/
